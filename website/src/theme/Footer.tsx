import React from 'react';
import Footer from '@site/src/components/Footer';

// Using the unified Footer component to replace the default Docusaurus footer
const CustomFooter: React.FC = () => {
  return <Footer />;
};

export default CustomFooter;
